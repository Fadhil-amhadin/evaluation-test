<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Navbar</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="#">Home</a>
          </li>
        </ul>
        <% if(!isLogin){ %> 
            <a href="/login" class="btn btn-outline-light m-1">Login</a>
            <a href="/register" class="btn btn-outline-light m-1">Register</a>
        <% } else {%> 
          <div class="dropdown">
            <button class="btn bg-transparent p-0" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <img src="/images/profile-logo.png" alt="avatar" class="avatar-img" width="50px" style="border-radius: 50%;">
            </button>
            <div class="dropdown-menu dropdown-profile" aria-labelledby="dropdownMenuButton">
              <a class="dropdown-item text-danger font-weight-bold" href="/logout">Log out</a>
            </div>
          </div>
        <% } %> 
      </div>
    </div>
  </nav>